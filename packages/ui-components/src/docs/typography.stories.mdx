import { Meta, Typeset } from '@storybook/addon-docs/blocks'
import createTheme from '@monorepo/design-tokens'

<Meta title="Design System/Typography" />

# Typography

**Font:** Roboto

**Sizes:** and **Weights:** 300(regular), 400(medium), 600(bold)

**Roboto Regular:**

<Typeset
  fontSizes={[
    Number(createTheme.typography.size.extraLarge),
    Number(createTheme.typography.size.large),
    Number(createTheme.typography.size.medium),
    Number(createTheme.typography.size.regular)
  ]}
  fontWeight={createTheme.typography.weight.regular}
/>

**Roboto Medium:**

<Typeset
  fontSizes={[
    Number(createTheme.typography.size.extraLarge),
    Number(createTheme.typography.size.large),
    Number(createTheme.typography.size.medium),
    Number(createTheme.typography.size.regular)
  ]}
  fontWeight={createTheme.typography.weight.medium}
/>

**Roboto Bold:**

<Typeset
  fontSizes={[
    Number(createTheme.typography.size.extraLarge),
    Number(createTheme.typography.size.large),
    Number(createTheme.typography.size.medium),
    Number(createTheme.typography.size.regular)
  ]}
  fontWeight={createTheme.typography.weight.bold}
/>
